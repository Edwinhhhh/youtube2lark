[project]
name = "yt2feishu"
version = "0.1.0"
description = "Convert YouTube captions to Markdown and optionally upload to Feishu Docs."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
yt2feishu = "yt2feishu.cli:main"

